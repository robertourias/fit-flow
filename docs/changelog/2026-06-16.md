# Changelog — 2026-06-16

## TASK13 — Dashboards de Progresso

**Commit:** `089e0a1`

### Backend (T1)
- `DashboardSummaryDto`: novos `DurationDataDto[]`, `semanalDuracao: number`, `HeatmapDataDto[]`
- `GetDashboardSummaryUseCase`: lookback 84 dias; duração via `endedAt - startedAt`; heatmap 84 entradas
- 4 novos testes unitários (duração com/sem `endedAt`, heatmap vazio, heatmap múltiplas sessões/dia)

### Shared Types (T2)
- `DurationDataDto`, `HeatmapDataDto` exportados; `DashboardSummaryDto` +3 campos; dist rebuild

### Navegação + Layout (T5)
- `nav-content.tsx`: `href: "/progress"` adicionado ao item "Progresso" (já existia sem href)
- `apps/web/src/app/progress/layout.tsx`: novo, wraps `<AppShell>`

### Componentes (T3)
- `DurationChart.tsx` + `DurationChartClient.tsx`: barras Recharts para duração semanal, `next/dynamic ssr: false`
- `ActivityHeatmapClient.tsx`: grade 12×7, 3 intensidades de cor (`bg-muted`/`bg-primary/40`/`bg-primary`), tooltip via `title` nativo
- 6 testes RTL (4 × heatmap cores, 2 × duration render)

### Página /progress (T4)
- `apps/web/src/app/progress/page.tsx`: Server Component, fetch único `/workout-sessions/summary`, layout 3 breakpoints

### Fix colateral (T6)
- 3 fixtures de TASK11 (`WorkoutActiveSession`, `WorkoutFinishForm`, `WorkoutStartPreview`) sem `workoutName` — corrigidos

---

## TASK12 — Histórico de Sessões

### Spec
- Gerado e aprovado: `docs/specs/2026-06-15-task12-historico-sessoes.md`
- Backlog atualizado: TASK12 `backlog` → `done`

### Backend (T1)
- `WorkoutSession` entity: campo `workoutName: string` + getter
- `PrismaWorkoutSessionsRepository`: `SESSION_INCLUDE` inclui `workout: { select: { name: true } }`; `toDomain` mapeia `row.workout.name`
- `WorkoutSessionSummaryDto`: campo `workoutName` + `fromEntity` mapping
- Testes: mocks existentes atualizados com `workoutName`, cobertura do novo campo

### Shared Types (T2)
- `packages/types/src/index.ts`: `WorkoutSessionSummaryDto.workoutName: string`

### Navegação + Layout (T5)
- `nav-content.tsx`: item "Histórico" (`History` icon) em `extraNavItems`, `href: "/history"`
- `AppShell.tsx`: `getActiveItem` + `sectionTitles` para `/history`
- Novo `apps/web/src/app/history/layout.tsx`

### Hooks (T3)
- `use-workout-sessions.ts`: `useWorkoutSessions()` via `useInfiniteQuery` (cursor-based, limit 20)
- `use-workout-session.ts`: `useWorkoutSession(id)` via `useQuery`
- `use-user-me.ts`: `useUserMe()` via `useQuery`
- Corrigido `workoutName` em mocks de `use-last-finished-session.test.tsx` e `use-create-workout-session.test.tsx`
- Testes: 3 novos arquivos cobrindo todos os hooks

### Frontend /history (T4)
- `apps/web/src/app/history/page.tsx`
- `apps/web/src/components/history/HistoryListPage.tsx`: lista paginada (load more), banner FREE 60d, empty state, skeleton
- Testes RTL (jest.mock dos hooks)

### Frontend /history/[id] (T6)
- `apps/web/src/app/history/[id]/page.tsx`
- `apps/web/src/components/history/SessionDetailPage.tsx`: cabeçalho (workoutName, data, duração, dificuldade, comentário), tabela de séries executadas por exercício (filtrando `completedAt` nulo), 404 handling
- Testes RTL (jest.mock dos hooks)

### Correções de teste descobertas no T7
- `__esModule: true` obrigatório em mocks de `next/link` para interop de default export (fix em `HistoryListPage.test.tsx` e `SessionDetailPage.test.tsx`)
- `act(() => { result.current.fetchNextPage(); })` + `waitFor` (não `await act(async)`) para `useInfiniteQuery` em testes

### Resultado final
- `apps/api`: 19 suites / 177 testes — tudo passando
- `apps/web`: 37 suites / 239 testes — tudo passando
- tsc/lint limpos
