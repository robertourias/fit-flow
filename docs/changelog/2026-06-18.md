# Changelog — 2026-06-18

## TASK17 — Área do Preparador: Alunos

**Commit:** `915af6c`

### Migration + Domínio (T1)
- `RelationshipInitiator { TRAINER, STUDENT }` (enum) + coluna `initiatedBy` (NOT NULL) em `TrainerStudentRelationship`
- `TrainerStudentRelationship` entity: `initiatedBy`, `wasInitiatedBy(userId)`
- `ITrainerStudentRelationshipRepository.findById`; `create()` exige `initiatedBy`
- `PATCH /users/me` aceita `isTrainer?: boolean`

### Use-cases de vínculo (T2)
- Módulo `apps/api/src/coaching` (importa `IdentityModule`)
- `InviteRelationshipUseCase` (FR-002/004/005, idempotência em `P2002`), `RespondRelationshipUseCase` (FR-006/007), `RevokeRelationshipUseCase`, `ListMyStudentsUseCase`, `ListMyTrainersUseCase`
- `RelationshipDto.fromEntity` (resolve nomes via `IUsersRepository`)

### Use-cases de rotina/progresso do aluno (T3)
- `CreateStudentStrategyUseCase`, `CreateStudentWorkoutUseCase`, `GetStudentDashboardUseCase` — todos checam `trainerHasAccessToStudent` (404 sem vínculo `ACTIVE`, FR-008), delegam ao use-case de Training com `tenantId = studentId`
- `TrainingModule` passa a exportar `CreateStrategyUseCase`/`CreateWorkoutUseCase`/`GetDashboardSummaryUseCase`

### Controllers + Swagger (T4)
- `CoachingController`: 7 endpoints da spec, Swagger documentado
- `coaching.e2e-spec.ts`: fluxo completo convite→aceite→criação rotina→dashboard→revoke + casos 400/403/404

### Frontend — toggle isTrainer (T5)
- `ProfilePageClient.tsx`: switch `isTrainer` (novo componente `ui/switch.tsx`), persistido via `updateProfile` action

### Frontend — hooks Coaching (T6)
- `apps/web/src/lib/api/hooks/`: `use-students`, `use-trainers`, `use-invite-relationship`, `use-respond-relationship`, `use-revoke-relationship`, `use-student-dashboard`, `use-create-student-strategy`, `use-create-student-workout`
- Mutations invalidam `["coaching","students"]`/`["coaching","trainers"]` ou `["coaching","student-dashboard", studentId]`

### Frontend — telas /students + nav (T7)
- `apps/web/src/app/students/page.tsx`: abas "Meus Alunos"/"Meus Preparadores" (`StudentsPage`, `RelationshipList`, `InviteRelationshipDialog`)
- `StudentDetailSheet`: dashboard do aluno (reaproveita componentes de gráfico do `/progress`) + `CreateStudentRoutineForm` (Strategy+Workout simplificado)
- `nav-content.tsx`: item "Alunos" em `extraNavItems`

### Resultado final
- `apps/api`: 32 suites / 264 testes — tudo passando
- `apps/web`: 63 suites / 349 testes — tudo passando
- tsc limpo nos dois apps; backlog TASK17 `spec-review` → `done`

### Nota de processo
- Sessão atingiu limite de uso no meio da Onda 4 (T4/T6 em paralelo); ambos subagentes já tinham implementado tudo antes do corte — retomada consistiu em verificar trabalho feito, completar teste faltante (`use-create-student-workout.test.tsx`) e marcar checkboxes.
