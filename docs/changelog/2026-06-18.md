# Changelog — 2026-06-18

## TASK17 — Área do Preparador: Alunos

**Commit:** `915af6c`

### Migration + Domínio (T1)
- `RelationshipInitiator { TRAINER, STUDENT }` (enum) + coluna `initiatedBy` (NOT NULL) em `TrainerStudentRelationship`
- `TrainerStudentRelationship` entity: `initiatedBy`, `wasInitiatedBy(userId)`
- `ITrainerStudentRelationshipRepository.findById`; `create()` exige `initiatedBy`
- `PATCH /users/me` aceita `isTrainer?: boolean`

### Use-cases de vínculo (T2)
- Módulo `apps/api/src/coaching` (importa `IdentityModule`)
- `InviteRelationshipUseCase` (FR-002/004/005, idempotência em `P2002`), `RespondRelationshipUseCase` (FR-006/007), `RevokeRelationshipUseCase`, `ListMyStudentsUseCase`, `ListMyTrainersUseCase`
- `RelationshipDto.fromEntity` (resolve nomes via `IUsersRepository`)

### Use-cases de rotina/progresso do aluno (T3)
- `CreateStudentStrategyUseCase`, `CreateStudentWorkoutUseCase`, `GetStudentDashboardUseCase` — todos checam `trainerHasAccessToStudent` (404 sem vínculo `ACTIVE`, FR-008), delegam ao use-case de Training com `tenantId = studentId`
- `TrainingModule` passa a exportar `CreateStrategyUseCase`/`CreateWorkoutUseCase`/`GetDashboardSummaryUseCase`

### Controllers + Swagger (T4)
- `CoachingController`: 7 endpoints da spec, Swagger documentado
- `coaching.e2e-spec.ts`: fluxo completo convite→aceite→criação rotina→dashboard→revoke + casos 400/403/404

### Frontend — toggle isTrainer (T5)
- `ProfilePageClient.tsx`: switch `isTrainer` (novo componente `ui/switch.tsx`), persistido via `updateProfile` action

### Frontend — hooks Coaching (T6)
- `apps/web/src/lib/api/hooks/`: `use-students`, `use-trainers`, `use-invite-relationship`, `use-respond-relationship`, `use-revoke-relationship`, `use-student-dashboard`, `use-create-student-strategy`, `use-create-student-workout`
- Mutations invalidam `["coaching","students"]`/`["coaching","trainers"]` ou `["coaching","student-dashboard", studentId]`

### Frontend — telas /students + nav (T7)
- `apps/web/src/app/students/page.tsx`: abas "Meus Alunos"/"Meus Preparadores" (`StudentsPage`, `RelationshipList`, `InviteRelationshipDialog`)
- `StudentDetailSheet`: dashboard do aluno (reaproveita componentes de gráfico do `/progress`) + `CreateStudentRoutineForm` (Strategy+Workout simplificado)
- `nav-content.tsx`: item "Alunos" em `extraNavItems`

### Resultado final
- `apps/api`: 32 suites / 264 testes — tudo passando
- `apps/web`: 63 suites / 349 testes — tudo passando
- tsc limpo nos dois apps; backlog TASK17 `spec-review` → `done`

### Nota de processo
- Sessão atingiu limite de uso no meio da Onda 4 (T4/T6 em paralelo); ambos subagentes já tinham implementado tudo antes do corte — retomada consistiu em verificar trabalho feito, completar teste faltante (`use-create-student-workout.test.tsx`) e marcar checkboxes.

---

## TASK18 — Personal (Comunicação)

**Commit:** `a765f61`

### Backend
- Models `Message`, `Notification` (enum `NotificationType`) + colunas `trainerLastReadAt`/`studentLastReadAt` em `TrainerStudentRelationship` (migration `20260618202020_add_message_notification`)
- `message-content-policy.ts`: blocklist PT-BR, regex word-boundary unicode-aware
- BullMQ (`bullmq`+`@nestjs/bullmq`): fila `"notifications"`, `NewMessageNotificationProcessor`
- `SendMessageUseCase` (modera → persiste → enfileira notificação, falha de enqueue não quebra o envio), `ListMessagesUseCase`, `MarkMessagesReadUseCase`
- `CreateNotificationUseCase`, `ListMyNotificationsUseCase`, `MarkNotificationReadUseCase`, `MarkAllNotificationsReadUseCase`
- `CoachingController` +3 endpoints de mensagens; `NotificationsController` novo (3 endpoints)
- `coaching.e2e-spec.ts`: fluxo mensagem→notificação→leitura + bloqueio por moderação (422)

### Frontend
- Hooks: `use-messages`, `use-send-message`, `use-mark-messages-read`, `use-notifications`, `use-mark-notification-read`, `use-mark-all-notifications-read` (polling via `refetchInterval`)
- `ChatPanel` no drawer de detalhe do aluno/preparador (`/students`)
- `NotificationBell` no header (badge de não lidas, dropdown, marca como lida)

### Resultado final
- `apps/api`: 46 suites / 329 testes; `apps/web`: 71 suites / 378 testes — tudo passando antes da TASK19 começar

### Nota de processo
- Dois subagentes (T3 mensagens, T7 UI) bateram limite de sessão a meio do trabalho — verificado código/testes no disco antes de marcar critérios, não confiado no relatório por si só.

---

## TASK19 — Hardening & Release

**Commit:** `a765f61`

### Cobertura (T1/T2)
- Baseline medido e travado como `coverageThreshold` (ratchet anti-regressão, não meta aspiracional): backend 88/63/78/89 (statements/branches/functions/lines), frontend 86/72/82/88
- Gaps fechados: `UpdateBodyMeasurementUseCase` (0% → testado), `use-exercises.ts` (16.7% → 100%)

### Observabilidade (T3/T4)
- Backend: `nestjs-pino` (logs JSON), `prom-client` (`GET /metrics` fora do prefixo `/api/v1`), `@sentry/nestjs` condicional a `SENTRY_DSN`
- Decorator `@RawResponse()` novo — `ResponseInterceptor` global envelopava `/metrics` em `{data,error}`, quebrando o formato de exposição Prometheus; rota de métricas agora escapa do envelope
- Frontend: `@sentry/nextjs` condicional a `NEXT_PUBLIC_SENTRY_DSN` (`instrumentation.ts`, `instrumentation-client.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`)

### Docker (T5)
- `apps/api/Dockerfile`: `prisma migrate deploy || exit 1` antes do `exec node dist/main.js` — falha de migration agora derruba o container em vez de seguir com schema inconsistente

### CI/CD (T6)
- `.github/workflows/ci.yml`: lint, typecheck (`apps/web` via `tsc --noEmit` explícito), test+coverage, build — todo PR/push
- `.github/workflows/deploy.yml`: Vercel + Railway via `workflow_dispatch`/push em `main`, skip limpo (sem falhar o workflow) quando `VERCEL_TOKEN`/`RAILWAY_TOKEN` ausentes

### Documentação (T7)
- `docs/architecture/infra.md` § CI/CD: lista de secrets + passo a passo do primeiro deploy manual

### Nota de processo
- Subagente da T3 criou os arquivos de observabilidade mas não plugou `MetricsModule`/`LoggerModule`/`initSentry()` em `app.module.ts`/`main.ts` antes de bater limite de sessão — wiring final feito manualmente, validado com `nest start` real contra Postgres/Redis locais (`curl /metrics` confirmando texto Prometheus puro fora do prefixo).
- Subagentes de T3, T4 e T6 bateram limite de sessão a meio do trabalho ao longo desta task — todo critério marcado `[x]` foi confirmado rodando testes/build reais, não apenas pelo relatório do subagente.
