# Changelog — 2026-06-11

## feat(backend): T1-T4 — hasOnboarded, empty exercises, dashboard summary, active workout

### Backend Implementation

**T1: hasOnboarded field**
- Added `hasOnboarded?: boolean` to `UpdateUserMeDto` 
- `UpdateMeUseCase` passes field through to repository
- Tests cover presence/absence of field (lines 89-108 in spec)

**T2: Empty exercises in CreateWorkoutDto**
- Removed `@ArrayMinSize(1)` constraint from `exercises` array
- Supports empty workouts for onboarding flow
- Test validates `exercises: []` creation (line 179-196)

**T3: GET /workout-sessions/summary (Dashboard endpoint)**
- Full aggregation algorithm: volume by day, streak detection, muscle group distribution
- `DashboardSummaryDto`: 9 fields (dias, treinos, delta, streak, volume, volumeData[], muscleGroups[], trainDates[], workoutsCount)
- Repository: `findFinishedSince(tenantId, since)` for efficient session fetching
- 8 unit tests + 3 e2e tests, all passing

**T4: GET /strategies/active-workout (Rotation logic)**
- Algorithm: `(finished sessions) % (workouts count)` → today's workout index
- Returns strategy name, today's workout (id/name/exercicios/order), next 2 workouts
- 6 unit tests + 4 e2e tests covering edge cases (no strategy, no workouts, single workout)

**Test Results:** 162 total API tests passing

### Frontend Implementation

**T5: TanStack Query + apiFetch infrastructure**
- `QueryProvider` in layout with `staleTime: 30s`
- `apiFetch<T>()`: server (direct to API), client (via `/api/proxy`)
- Shared DTOs in `packages/types`
- 6 unit tests for `apiFetch`

**T6: Onboarding wizard (3-step form)**
- ProfileStep (name, age, bio) → GoalsStep (multi-select) → SplitStep (split type + program name)
- `useCompleteOnboarding` mutation: PATCH /users/me + POST /strategies + N×POST /workouts
- JWT callback: `trigger === "update"` syncs `session.hasOnboarded`
- Middleware: auto-redirect `/onboarding` ↔ `/dashboard` based on `hasOnboarded`
- Button: added `isLoading` prop with spinner
- Split presets: ABC, Upper/Lower, PPL, Full Body

**T7: Dashboard real data (async Server Component)**
- Parallel fetches: `/users/me` + `/workout-sessions/summary` + `/strategies/active-workout`
- Maps real data to component shapes
- TreinoCard: empty state with CTA to `/onboarding`
- No mock data imports

**T8+T9 Hooks Layer (Ready for integration)**
- T8: `useExercises(filters)`, `useExercise(id)`, `useMuscleGroups()`, `useEquipment()`
- T9: `useStrategies()`, `useStrategy(id)`, `useUpdateStrategy()`, `useDeleteStrategy()`
- Utility: `programColor(id)` deterministic hash-based palette

### UI Components

**T8+T9 Components**
- `DropdownMenu`: Radix-based dropdown with animations
- `AlertDialog`: Confirmation dialog with header/footer/actions
- `ProgramOptionsMenu`: Ativar/Desativar/Excluir com confirmação

### Tests & Quality

- All 162 API tests passing
- T6 wizard validated: form state, mutations, session update
- T7 dashboard: real data mapping, empty state
- No blocking issues; all acceptance criteria met for completed tasks

### Remaining Work (T8+T9 UI Integration)

1. `/exercises/page.tsx` — integrate `useExercises()` with pagination
2. `/exercises/[id]/page.tsx` — use `useExercise(id)` with 404 handling
3. `/library/page.tsx` — replace mock with `useStrategies()`
4. `/program/[id]/page.tsx` — Server Component with `useStrategy(id)`
5. `ProgramHeader.tsx` — add ProgramOptionsMenu + programColor background

Integration points are clear; hooks provide strong type safety via shared DTOs.
