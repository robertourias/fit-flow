# Changelog — 2026-06-01

## feat(web): fluxo completo de detalhe e execução de treino (T-1 a T-9)

### Adicionado
- **Dependências:** `zustand` e `@dnd-kit/core/sortable/utilities` em `apps/web`
- **Mock data:** `lib/mock/workout.ts` — 6 treinos (A-LOWER-1 … F-OMBRO-BRACOS), tipos `PlannedSet`, `WorkoutExercise`, `WorkoutDetail`, `ExecutedSet`, `ExecutedExercise`, `WorkoutSession`, `mockLastSessions`
- **Store:** `lib/stores/workout-session.store.ts` — Zustand com persist no localStorage; actions: `startSession`, `completeSet`, `setRestTimer`, `clearRest`, `setCurrentExercise`, `updateNote`, `beginFinishing`, `resetSession`
- **Rotas:**
  - `/workout/[id]/(detail)` — layout com AppShell + page (WorkoutDetailPage)
  - `/workout/[id]/start` — WorkoutStartPreview
  - `/workout/[id]/session` — WorkoutActiveSession
  - `/workout/[id]/finish` — WorkoutFinishForm
- **WorkoutDetailPage** — lista colapsável de exercícios, drag-and-drop via @dnd-kit/sortable, tabela de séries editável, botão "+ Adicionar série/exercício", SuggestionsPanel (desktop 320px)
- **WorkoutStartPreview** — última execução, total de séries, duração estimada, lista de exercícios com carga anterior, CTA fixo
- **WorkoutActiveSession** — timer (reconstrói de `startedAt` pós-refresh), progress bar, tabela Série/Anterior/Kg/Reps/✓, rest countdown com "Pular", next exercise preview no rodapé
- **WorkoutFinishForm** — textarea "Como foi?", botão fotos (UI-only), dificuldade (1-5 estrelas), accordion de detalhes, toggle "Atualizar rotina" (ativo por padrão), integrações Strava/Health Connect (UI-only), CTA "Salvar Treino" fixo

### Modificado
- **WorkoutCard** e **WorkoutListRow** convertidos de `div` para `Link` (`next/link`) apontando para `/workout/${workout.id}`
- **AppShell:** `pathname.startsWith("/workout")` → item ativo `"biblioteca"`

### Spec / Plano
- `docs/specs/2026-06-01-workout-detail.md` (Status: approved)
- `docs/plans/2026-06-01-workout-detail.md`
