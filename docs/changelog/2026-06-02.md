# Changelog — 2026-06-02

## feat(web): performance — React.memo, useDeferredValue, react-window, next/dynamic

- `ExerciseCard`, `WorkoutCard`, `WorkoutListRow` → `React.memo`
- `useDeferredValue(search)` + `useDeferredValue(filtered)` em `ExercisesClientPage` (remove `useDebounce`)
- `react-window` `FixedSizeList` em exercises list (ativa com ≥30 itens; `ResizeObserver` para height; media query para column count)
- `ProgressChartClient` wrapper Client Component → `next/dynamic({ ssr: false })` para recharts (~80KB) fora do bundle inicial
- `useMemo([isDark])` em `useChartColors` para estabilizar objeto de cores
- `@next/bundle-analyzer` configurado (`ANALYZE=true pnpm build`)

## fix(web): webpack build — ssr:false proibido em Server Component

- `ProgressChartClient.tsx` criado como wrapper Client Component
- `dashboard/page.tsx` importa o wrapper (não o `next/dynamic` direto)

## feat(spec): modelagem de dados — Identity + Catálogo + Training

- `docs/specs/2026-06-01-data-model.md` aprovado
- `docs/plans/2026-06-02-data-model.md` executado na íntegra (T-1 a T-11)

## feat(db): packages/db — schema Prisma + migration inicial

- `packages/db/` criado: `prisma/schema.prisma` (16 models, 4 enums), `src/index.ts` (singleton)
- Migration `20260602204914_init` aplicada no banco Docker (porta 5433)
- PostgreSQL porta alterada para 5433 (conflito com PostgreSQL nativo Windows na 5432)
- Root `.env` criado com `DATABASE_URL`, `POSTGRES_PORT=5433`, `AUTH_SECRET`
- `pg_hba.conf` do container alterado de `scram-sha-256` para `md5`

## feat(api): domain entities e repository interfaces (T-2 a T-7)

**Identity** (`apps/api/src/identity/domain/`):
- `User`, `TrainerStudentRelationship` entities + `Plan`, `RelationshipStatus` enums
- `IUsersRepository`, `ITrainerStudentRelationshipRepository` interfaces

**Catalog** (`apps/api/src/catalog/domain/`):
- `Exercise`, `MuscleGroup`, `Equipment` entities + `ExerciseCategory` enum
- `IExercisesRepository` interface (findMany com OR tenantId NULL/tenant)

**Training** (`apps/api/src/training/domain/`):
- `Strategy`, `Workout`, `WorkoutExercise`, `WorkoutSession`, `SessionExercise` entities
- `PlannedSet`, `ExecutedSet` value objects + `WorkoutSessionStatus` enum
- `IStrategiesRepository`, `IWorkoutsRepository`, `IWorkoutSessionsRepository`

## feat(api): módulos NestJS + Prisma repositories (T-8 a T-10)

- `PrismaUsersRepository`, `PrismaTrainerStudentRelationshipRepository` → `IdentityModule`
- `PrismaExercisesRepository` → `CatalogModule`
- `PrismaStrategiesRepository` (setActive via $transaction), `PrismaWorkoutsRepository` (FREE plan limit), `PrismaWorkoutSessionsRepository` → `TrainingModule`
- DI tokens como Symbols (`USERS_REPOSITORY`, `EXERCISES_REPOSITORY`, etc.)

## feat(web): NextAuth v5 + PrismaAdapter (T-11)

- `next-auth@beta`, `@auth/prisma-adapter`, `@auth/core` adicionados a `apps/web`
- `src/lib/auth.ts` + `src/app/api/auth/[...nextauth]/route.ts` criados
- Schema: `AuthSession` → `Session` (tabela `auth_sessions` inalterada via `@@map`)
- TS2742 workaround: `auth`/`signIn` anotados como `any` (next-auth v5 beta, pnpm monorepo)
